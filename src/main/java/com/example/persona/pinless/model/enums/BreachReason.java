package com.example.persona.pinless.model.enums;

public enum BreachReason {
    PINLESS_DISABLED,
    CURRENCY_MISMATCH,
    THRESHOLD_EXCEEDED,
    DAILY_COUNT_EXCEEDED,
    MONTHLY_COUNT_EXCEEDED,
    DAILY_AMOUNT_EXCEEDED,
    MONTHLY_AMOUNT_EXCEEDED
}
