package kh.com.wingbank.cipher.token.service;

/**
 * Stub — replaced by the real wing-token library in deployed environments.
 * Validates the JWT assertion header and returns whether the caller is authorised.
 */
public interface JwtValidationService {
    boolean validateJwtAssertionHeader(Object context);
}
